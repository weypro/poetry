//判断当前记录是否存在，0不存在，1存在
const isExisted = 0;
//判断数据是否select完成，0还未select，1完成，-1失败
const isSelectReady = 0;
//判断数据是否excute完成，0还未select，1完成，-1失败
const isExcuteReady = 0;
//引出数组
const allPoetryList=new Array();

//导出全局变量
export default {
	isExisted,
	isSelectReady,
	allPoetryList
}
